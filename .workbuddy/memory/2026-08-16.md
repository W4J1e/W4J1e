# 2026-08-16

个人主页 W4J1e 改动记录：
- 流星方向修正：`.comet` 渐变改为 `linear-gradient(90deg, transparent, var(--accent-color))`，从左往右飞、亮头在前（尾巴在身后）。
- 字体优化：正文改系统中文黑体栈（零下载、瞬时）；仅 `.about-card` 保留 Noto Serif SC，host 换 `fonts.loli.net`（woff2 走 `gstatic.loli.net`，绕开慢的 gstatic.com）。删除从未被 CSS 引用的 ZCOOL KuaiLe。geekzu 实测仍指向 gstatic.com，已排除。
- 作品卡片：6 张（simpleNav / video2livephoto / 21note / memox / memox_web / lightrans）链接改 GitHub `github.com/W4J1e/<项目名>`；改为横向流动 marquee（悬停暂停），卡片缩小为 200px；图标替换：simpleNav→img/simplenav.ico，memox+memox_web→img/memox.png，lightrans→img/lightrans.png，video2livephoto→📷（待用户出 ico 再换），21note→📝 不变。marquee 由 script.js 复制一份卡片实现无缝循环；卡片移出淡入观察器避免冲突。
- 备注：用户口误提到的 "lightrans_web" 卡片实际不存在，按 memox/memox_web 共用 memox.png 处理。
